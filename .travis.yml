language: node_js

node_js:
  - "lts/*"

script:
  - npm run build

before_deploy: "cd dist/${TRAVIS_TAG//@[0-9].[0-9].[0-9]}"

deploy:
  provider: npm
  skip_cleanup: true
  email: $NPM_EMAIL
  api_key: $NPM_AUTH_TOKEN
  on:
    branch: master
    tags: true
